# PostgreSQL 데이터베이스 URL (Render에서 제공)
DATABASE_URL=postgresql://username:password@host:port/database

# 또는 로컬 개발용
# DATABASE_URL=postgresql://localhost:5432/pt_record_system

# 포트 (Render에서 자동 설정)
PORT=3000

# 환경 (production/development)
NODE_ENV=production
